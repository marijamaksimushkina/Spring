db:
  username: test
  password: test

  spring:
    datasource:
        username: test
        password: test
        driver-class-name: org.h2.Driver
      jpa:
        properties.hibernate:
          hbm2ddl.auto: validate

